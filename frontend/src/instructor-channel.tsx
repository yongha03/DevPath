import InstructorChannelApp from './InstructorChannelApp'
import { renderPage } from './render-page'

renderPage(<InstructorChannelApp />, { missingRootMessage: 'instructor-channel root element was not found' })

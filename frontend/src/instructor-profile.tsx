import InstructorChannelApp from './InstructorChannelApp'
import { renderPage } from './render-page'

renderPage(<InstructorChannelApp />, { missingRootMessage: 'instructor-profile root element was not found' })
